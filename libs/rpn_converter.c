
#include <stdio.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "rpn_converter.h"


int*to_rpn(int*tokens){
    //Token*rpn;
    //int*not;
    //*not=-1;
    //tokens=remove_unary(tokens);
    //Expr_t*expr=make_expr(tokens)
    //int b=find_high_op(tokens,not);
    for(float i=0;i<10;i+=0.1){
        printf("%d\n",*((int*)(&i)));
    }
    return tokens;
    
}