t0 = time();
for (i from 3000 to 10000){
	is_prime = 1b
	for(k from 2 to i / 2 + 1){
		if (!(i % k)){
			is_prime = 0b
			stop
		}
	}
	if (is_prime){
		println(i)
	}
}
println("Time:", time() - t0, "ms")
