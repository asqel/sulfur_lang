win: build the interpreter and the built-in libs for windows

linux: build the interpreter and the built-in libs for windows

run: run the interpreter with no arg

stdlibs_win: build std libs in .dll for windows

stdlibs_linux: build std libs in .so for linux
	
win_all: build interpreter and std libs and run for windows

linux_all: build interpreter and std libs and run for linux

info: show this message

help: show this message
